---
title: "ポーリング設定の基本"
---
# ポーリング設定の基本

TWSNMP FC および TWSNMP FK における、ポーリング設定の基本的な操作と画面の入力項目について解説します。

## 1. ポーリング設定画面の開き方

ポーリングの設定は、各ノードの「ポーリングリスト」画面から行います。

- **新規追加**: ポーリングリスト画面の上部にある `<追加>` ボタンをクリックすると、新しいポーリングを作成するための設定画面が開きます。
- **既存の編集**: リストに表示されている既存のポーリング行にある `<編集>` ボタン（またはアクションメニューから編集を選択）をクリックすると、そのポーリングの設定内容を編集できます。

### TWSNMP FC

#### ポーリングリスト画面
![TWSNMP FC ポーリングリスト](/images/books/twsnmp-polling-dictionary/TWSNMPFC_POLLING_LIST.png)

#### ポーリング設定画面
![TWSNMP FC ポーリング設定](/images/books/twsnmp-polling-dictionary/TWSNMPFC_POLLING_EDIT.png)

### TWSNMP FK

#### ポーリングリスト画面
![TWSNMP FK ポーリングリスト](/images/books/twsnmp-polling-dictionary/TWSNMPFK_POLLING_LIST.png)

#### ポーリング設定画面
![TWSNMP FK ポーリング設定](/images/books/twsnmp-polling-dictionary/TWSNMPFK_POLLING_EDIT.png)

> [!IMPORTANT]
> 各ポーリング種別の詳細な解説記事（01以降）では、設定画面のスクリーンショットは省略し、ポーリング結果（グラフや状態）のサンプル画像のみを掲載しています。設定画面のUIはこの記事を参考にしてください。

## 2. ポーリング設定画面の入力項目

ポーリング設定画面には、主に以下の入力項目があります。（※選択した「種別」によって表示される項目が一部異なります）

- **名前 (Name)**
  - ポーリングの識別名です。リスト上で分かりやすい名前を入力します。（例: `PING死活監視`, `CPU使用率`）
- **種別 (Type)**
  - ポーリングの種類を選択します（`ping`, `snmp`, `http` など）。この選択によって、後続の入力フィールドや動作が切り替わります。
- **モード (Mode)**
  - 種別の中での詳細な動作モードを指定します。（例: SNMPポーリングにおける `traffic` や `sysUpTime` など）
- **パラメータ (Params)**
  - モードの実行に必要な追加情報を指定します。指定フォーマットは種別やモードによって異なります（例: OIDやポート番号、URLなど）。
- **フィルター (Filter)**
  - 取得した結果から特定の文字列や条件に合致するものだけを抽出（または除外）するための正規表現を指定します。
- **抽出 (Extractor)**
  - 主にHTTPやコマンド実行などで、複雑な応答データ（JSONやHTMLなど）から特定の値を抜き出すための条件（JSONPathやGrokパターンなど）を指定します。
- **スクリプト (Script)**
  - 取得したデータを基に、正常か異常かを判定するためのJavaScriptロジックを記述します。最終的に `true` なら正常、`false` なら設定した「障害レベル」の状態になります。
- **障害レベル (Level)**
  - ポーリング（スクリプトでの判定）が失敗した際に、ノードをどの状態にするか（`high`: 重度, `low`: 軽度, `warn`: 警告など）を指定します。
- **ログモード (Log Mode)**
  - ポーリングの実行結果をどのようにログに記録するか（常に記録、状態変化時のみ、AI分析対象とするかなど）を指定します。
- **説明 (Description)**
  - このポーリングの目的や運用上のメモを自由に記述できる備忘録フィールドです。
